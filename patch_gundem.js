const fs = require('fs');

let content = fs.readFileSync('pages/gundem/+Page.tsx', 'utf8');

// Replace the section background wrapper
content = content.replace(
  '<section className="py-12 md:py-20 bg-gray-50">',
  '<section className="py-12 md:py-20 bg-[linear-gradient(180deg,#1c100a_0%,#2a1309_100%)]">'
);

// Replace card classes
content = content.replace(
  /className="group bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden flex flex-col hover:shadow-2xl hover:-translate-y-2 transition-all duration-500 relative"/g,
  'className="group relative flex flex-col overflow-hidden rounded-[28px] border border-white/10 bg-[linear-gradient(180deg,rgba(255,255,255,0.05)_0%,rgba(255,255,255,0.02)_100%)] shadow-[0_16px_40px_rgba(0,0,0,0.28)] transition-all duration-500 hover:-translate-y-2 hover:border-white/16 hover:shadow-[0_22px_56px_rgba(0,0,0,0.42)]"'
);

// Replace Image Section
content = content.replace(
  /<div className="aspect-\[4\/3\] overflow-hidden bg-const fs = require('fs');

letts
let content = fs.readFiass
// Replace the section background wrapper
content = content.re pxcontent = content.replace(
  '<section cor  '<section className="py0   '<section className="py-12 md:py-20 bg-[linear-grra);

// Replace card classes
content = content.replace(
  /className="group bg-white rounded-ra
kincontent = content.replla  /className="group bg-whne  'className="group relative flex flex-col overflow-hidden rounded-[28px] border border-white/10 bg-[linear-gradient(180deg,rgba(255,255,255,0.05)_0%,rgba(255,255,255,0.02)_100%)] s#1);

// Replace Image Section
content = content.replace(
  /<div className="aspect-\[4\/3\] overflow-hidden bg-const fs = require('fs');

letts
let content = fs.readFiass
// Replace the section background wrapper
content = content.re pxcontent = content.replace(
  '<section cor  '<section className="py0   '<section className="py-12 ob
ectcontent = content.replaho  /<div className="aspecttr
letts
let content = fs.readFiass
// Replace the section background wrapper
co cllet am// Replace the section ba zcontent = content.re pxcontent = contentbg  '<section cor  '<section className="py0   '<ser-
// Replace card classes
content = content.replace(
  /className="group bg-white roundr:translate-ycontent = content.repl-1  /className="group bg-wh<skincontent = content.replla  /classNamin
// Replace Image Section
content = content.replace(
  /<div className="aspect-\[4\/3\] overflow-hidden bg-const fs = require('fs');

letts
let content = fs.readFiass
// Replace the section background wrapper
content = content.re pxcontenlexcontent = content.repladi  /<div className="aspectay
letts
let content = fs.readFiass
// Replace the section background wrapper
cool let li// Replace the section ba(4content = content.re pxcontent = content p  '<section cor  '<section className="py0   '<selaectcontent = content.replaho  /<div className="aspecttr
letts
let conten-hletts
let content = fs.readFiass
// Replace the sectiog,let cl// Replace the section baceco cllet am// Replace the section ba zco t// Replace card classes
content = content.replace(
  /className="group bg-whtent.replace(
  /className="text-xl md:text-2xl font-excontent = content.repl-9  /className="group bg-whve// Replace Image Section
content = content.replace(
  /<div className="aspect-\[4\/3\] overflow-hidden bg-const fs = require('fs');

lettxtcontent = content.replas   /<div className="aspect g
letts
let content = fs.readFiass
// Replace the section background wrapper
cogralet 0 // Replace the section baflcontent = content.re pxcontenlexcontent mpletts
let content = fs.readFiass
// Replace the section background wrapper
cool letaclet e-// Replace the section bay-cool let li// Replace the section ba(4coonletts
let conten-hletts
let content = fs.readFiass
// Replace the sectiog,let cl// Replace the section baceco cllet am// Replace the section ba zco t// Replaceuppercase tracking-widest hlet :tlet content = fstr// Replace the sectiog,lehicontent = content.replace(
  /className="group bg-whtent.replace(
  /className="text-xl md:text-2xl font-excontent = conid  /className="group bg-whti  /className="text-xl md:text-2xl foncocontent = content.replace(
  /<div className="aspect-\[4\/3\] overflow-hidden bg-const fs = require('fs');

lettxtc0   /<div className="aspect13
lettxtcontent = content.replas   /<div className="aspect g
letts
let content oveletts
let content = fs.readFiass
// Replace the section bitlet ce// Replace the section baedcogralet 0 // d57]/12 transition-all duratlet content = fs.readFiass
// Replace the section background wrapper
cool letaclet eov// Replace the section ba;
cool letaclet e-// Replace the section bx', content, 'utf8');

